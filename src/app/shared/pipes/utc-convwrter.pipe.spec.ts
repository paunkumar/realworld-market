import { UtcConvwrterPipe } from './utc-convwrter.pipe';

describe('UtcConvwrterPipe', () => {
  it('create an instance', () => {
    const pipe = new UtcConvwrterPipe();
    expect(pipe).toBeTruthy();
  });
});
